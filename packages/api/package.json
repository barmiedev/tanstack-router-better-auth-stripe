{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "hono": "^4.7.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "eslint": "^9.26.0",
    "prettier": "^3.5.3"
  }
}
