{
  "name": "tanstack-router-better-auth-stripe",
  "private": true,
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "preview": "bun --filter '*' preview",
    "start": "bun --filter '*' start",
    "lint": "bun --filter '*' lint",
    "format": "bun --filter '*' format"
  },
  "workspaces": [
    "packages/*"
  ]
}
